# Redirects

Use `httpx.NewRedirectResponse` to send an HTTP redirect.

```go
return httpx.NewRedirectResponse("/login", http.StatusFound)
```

The helper ensures the status code is valid and sets the `Location` header for you.
