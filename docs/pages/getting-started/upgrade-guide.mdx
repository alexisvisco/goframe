# Upgrade Guide

Before upgrading, update the CLI to the newest version:

```bash
go install github.com/alexisvisco/goframe/cli/goframe@latest
```

After updating the dependencies in your `go.mod`, run the project tests and rebuild your application.
